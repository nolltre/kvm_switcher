[project]
name = "kvm-switcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hid>=1.0.6",
    "pyusb>=1.2.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
